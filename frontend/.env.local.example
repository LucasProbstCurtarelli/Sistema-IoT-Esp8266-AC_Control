# Copy to .env.local when developing Next locally (optional).
# Empty NEXT_PUBLIC_API_URL = same-origin + next.config rewrites to Spring (recommended with Docker backend on :8080).

NEXT_PUBLIC_API_URL=

# Optional tuning
# NEXT_PUBLIC_API_TIMEOUT=10000
